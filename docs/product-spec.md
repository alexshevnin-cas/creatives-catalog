# Creatives Catalog — Продуктовая спецификация

> **Версия:** 1.0
> **Дата:** 2026-03-18
> **Статус:** Прототип готов, передано в разработку
> **Прототип:** https://alexshevnin-cas.github.io/creatives-catalog/
> **ТЗ для команды:** `docs/technical-spec.md`
> **Источники:** интервью Novatska (2026-03-12), Borys Shyfrin (2026-03-16), валидация на прототипе

---

## 1. Проблема

Рекламные креативы (видео, баннеры, playables) управляются вручную: дизайнеры сдают файлы → координатор переименовывает → заливает на рекламные сети. Нет единого каталога, нет автоматизации нейминга, нет связи с аналитикой.

**Последствия:**
- Аналитика в Superset ломается из-за неконсистентного нейминга
- Координатор тратит время на ручное переименование и заливку
- Менеджеры не видят превью креативов в контексте перформанса
- Нет единого реестра: что создано, что запущено, что работает

---

## 2. Цели

1. **Единый каталог** креативов с поиском, превью и метаданными
2. **Автоматический нейминг** по конвенции при загрузке — дизайнер выбирает из дропдаунов, система именует
3. **Google Drive как хранилище** — не ломаем существующий процесс, интегрируемся
4. **Связка с аналитикой** — имя в каталоге = имя в Superset
5. **Снижение ручного труда** координатора — убираем из цепочки нейминга

---

## 3. Пользователи

| Роль | Что делает в каталоге | Текущий инструмент |
|------|----------------------|-------------------|
| **Креативщик** (8+ чел.) | Загружает файл, выбирает игру + тип + сезон из дропдаунов | Папки на Google Drive |
| **Координатор** (Hanna) | Видит готовые креативы с правильными именами, заливает на сети | Ручное переименование |
| **UA-менеджер** (Zoriana + команда) | Смотрит каталог с превью, отмечает сети | Superset + папки |
| **Менеджер игры** | Решает, какой креатив куда залить, видит статусы | Чат с координатором |

---

## 4. Нейминговая конвенция

### Правила
1. Только латиница (ASCII)
2. Разделитель: `_` (нижнее подчёркивание)
3. Без пробелов, без кириллицы
4. Формат зависит от типа креатива
5. **Сеть НЕ включать в имя** — источник определяется через MMP/API
6. **Сезонность — фиксированная позиция** сразу после номера, без подчёркивания
7. **Размеры и длительность определяются автоматически** из загруженного файла

### Имя концепта (без размеров)

| Тип | Шаблон | Пример |
|-----|--------|--------|
| Video | `V{NNN}{seasonal}_{код_1С}_{shortname}` | `V003UE_7448_CarCrash` |
| Banner | `B{NNN}{seasonal}_{код_1С}_{shortname}` | `B011_7448_CarCrash` |
| Playable | `PLAY_{NNN}{seasonal}_{код_1С}_{shortname}` | `PLAY_001_7448_CarCrash` |

### Имя рендиции (полное имя файла)

| Тип | Шаблон | Пример |
|-----|--------|--------|
| Video | `{concept}_{WxH}_{duration}s` | `V003UE_7448_CarCrash_1920x1080_30s` |
| Banner | `{concept}_{WxH}` | `B011_7448_CarCrash_1020x500` |
| Playable | `{concept}` | `PLAY_001_7448_CarCrash` |

### Сезонные маркеры

| Код | Значение |
|-----|----------|
| STD | Стандартный (не отображается в имени) |
| UE | Universal Event (сезонное промо) |
| NY | New Year |
| EA | Easter |

Формат: суффикс сразу после номера без подчёркивания — `V003UE_...`. Для STD суффикс пустой — `V003_...`.

### Иерархия сущностей

```
Game (код 1С = 7448, "Car Crash Simulator")
 └── Creative Concept (V003 = 3-я идея)
      └── Rendition (1920x1080_30s, 1080x1920_30s, 1280x720_15s)
```

**Concept** — единица творческой идеи (один порядковый номер).
**Rendition** — адаптация под формат (разные разрешения/длительности одного концепта).

---

## 5. Модель данных

### Game
| Поле | Тип | Описание |
|------|-----|----------|
| id | int | PK |
| code_1c | string | Код из 1С (e.g. "7448"), UNIQUE |
| name | string | Полное название игры |
| short_name | string | Сокращение для нейминга (e.g. "CarCrash") |
| platform | enum | Android / iOS / Both |

### Creative (Concept)
| Поле | Тип | Описание |
|------|-----|----------|
| id | int | PK |
| game_id | FK → Game | Привязка к игре |
| type | enum | Video / Banner / Playable |
| seq_number | int | Порядковый номер, автоинкремент per game+type |
| seasonal_tag | enum | STD / UE / NY / EA |
| concept_name | string | Сгенерированное имя ("V003UE_7448_CarCrash") |
| status | enum | Draft / Ready / Active / Archived |
| networks | string | Сети, куда залито ("Mintegral,FB,TikTok,Google Ads") |
| created_at | datetime | Дата создания |
| created_by | FK → User | Креативщик |
| | | UNIQUE(game_id, type, seq_number) |

### Rendition
| Поле | Тип | Описание |
|------|-----|----------|
| id | int | PK |
| creative_id | FK → Creative | Привязка к концепту (CASCADE DELETE) |
| width | int | Ширина (px), авто-определение |
| height | int | Высота (px), авто-определение |
| duration_sec | int? | Длительность (для видео), авто-определение |
| generated_name | string | Полное имя файла ("V003UE_7448_CarCrash_1920x1080_30s") |
| drive_file_id | string | Google Drive file ID |
| drive_thumbnail_url | string? | Ссылка на превью из Drive API |
| file_size_mb | float | Размер файла |

---

## 6. Целевой процесс

### Сейчас
```
Дизайнер → файл с произвольным именем
         → Hanna переименовывает вручную
         → Google Drive (папки по играм)
         → Hanna вручную заливает на сети
         → API сетей → Superset (нейминг иногда ломается)
```

### Целевой (V1): Каталог + Google Drive
```
Дизайнер → Каталог (форма: [Игра ▼] [Тип ▼] [Сезон ▼] + файл)
         → Система автоименует + определяет размеры из файла
         → Файл загружается на Google Drive в правильную папку
         → Thumbnail подтягивается из Drive API
         → Менеджер отмечает сети в каталоге
         → Координатор видит готовые файлы с правильными именами, заливает на сети
         → MMP → Superset (нейминг всегда корректный)
```

### Целевой (V2): Автозаливка
```
Дизайнер → загружает в Catalog
         → Менеджер отмечает сети + кампании
         → Система автоматически заливает через API сетей
         → Performance data возвращается в Catalog
```

---

## 7. UI каталога

### Древовидная структура

```
▸ CarCrash (7448)
  ▸ Video                                         [12]  [+ Добавить]
    | #   | Сезон | Превью | Концепт              | Файлы     | Сети                    | Статус   |
    | 012 | STD   | [img]  | V012_7448_CarCrash   | 2 файла ▸ | [Mintegral] [FB] [...]  | Draft    |
    | 011 | STD   | [img]  | V011_7448_CarCrash   | 3 файла ▸ | [Mintegral] [FB] [...]  | Active   |
    ...
                                                     └── V011_7448_CarCrash_1920x1080_30s  [Скачать]
                                                         V011_7448_CarCrash_1080x1920_30s  [Скачать]
                                                         V011_7448_CarCrash_1280x720_15s   [Скачать]
  ▸ Banner                                        [12]  [+ Добавить]
  ▸ Playable                                      [12]  [+ Добавить]
```

### Принципы UI
- Сортировка: новые концепты сверху (DESC по номеру)
- Фильтры: игра, тип, сезон, статус, поиск по имени
- Сети — кликабельные бейджи (toggle on/off)
- Статус — дропдаун inline
- Файлы — раскрывающийся список рендиций
- Кнопка "+ Добавить" — модалка с предзаполненными игрой и типом
- Превью — thumbnails из Google Drive API

### Форма добавления (модалка)
- Три дропдауна в одну строку: Игра, Тип, Сезонность (предзаполнены если открыто из каталога)
- Drag & drop файла
- Размеры и длительность определяются из файла автоматически
- Live-preview сгенерированного имени
- Кнопка "Добавить"

---

## 8. Google Drive интеграция

### Хранилище
Google Drive — основное хранилище файлов. Не заменяем, а интегрируемся.

### Структура папок
```
[Creatives]/
├── CarCrash (7448)/
│   ├── Video/
│   │   ├── V001_7448_CarCrash/
│   │   │   ├── V001_7448_CarCrash_1920x1080_30s.mp4
│   │   │   └── V001_7448_CarCrash_1080x1920_30s.mp4
│   │   └── V002UE_7448_CarCrash/
│   ├── Banner/
│   └── Playable/
```

### Thumbnails
Google Drive API отдаёт `thumbnailLink` для видео и изображений — не нужен ffmpeg, не нужно хранить превью локально.

---

## 9. MVP Scope

### V1 (в разработке)
- [x] Древовидный каталог: Игра → Тип → Концепт → Рендиции _(прототип)_
- [x] Автонейминг по конвенции _(прототип)_
- [x] Авто-определение размеров и длительности из файла _(прототип)_
- [x] Фильтры, поиск, статусы _(прототип)_
- [x] Отметка сетей (Mintegral, FB, TikTok, Google Ads) _(прототип)_
- [ ] Google Drive интеграция (upload + thumbnails)
- [ ] Google OAuth 2.0
- [ ] PostgreSQL вместо SQLite
- [ ] Справочник игр (код 1С + short_name)

### V2+
- Автозаливка на рекламные сети через API
- Перформанс-данные из Superset в карточке креатива
- A/B-группы с автоматическим запуском
- Drive watcher (фоновый импорт файлов из Drive)
- Маппинг старых креативов
- Интеграция с 1С для автосинхронизации справочника игр

---

## 10. Принятые решения

| # | Решение | Источник | Дата |
|---|---------|----------|------|
| D1 | Сеть НЕ включать в нейминг — источник из MMP | Shyfrin | 2026-03-16 |
| D2 | Сезонность — фиксированная позиция после номера | Shyfrin | 2026-03-16 |
| D3 | Старые креативы не переименовывать в сетях | Shyfrin + Alexei | 2026-03-16 |
| D4 | Сезонная вариация = новый концепт (новый порядковый номер) | Shyfrin | 2026-03-16 |
| D5 | Google Drive как хранилище, не заменяем | Alexei | 2026-03-18 |
| D6 | Thumbnails из Drive API, не храним локально | Alexei | 2026-03-18 |
| D7 | Три типа: Video, Banner, Playable (StoreVideo убран) | Валидация на прототипе | 2026-03-18 |
| D8 | Размеры и длительность — авто-определение из файла | Валидация на прототипе | 2026-03-18 |

---

## 11. Открытые вопросы

1. ~~**Каталог или бот?**~~ → Каталог + Google Drive интеграция (решено на прототипе)
2. **Лимиты длины имени** по рекламным сетям (Meta, Google Ads, Unity, Mintegral, TikTok) — нужно исследование
3. **Zoriana Omelchuk (CMO)** — показать прототип, подтвердить скоуп, верифицировать процесс
4. **Топовый UA-менеджер (Виталий)** — верифицировать реальный процесс на уровне исполнителей
5. **PlayTurbo/Mintegral playables** — загружаются отдельным путём, как учитывать?
6. **Права доступа** — кто может загружать, кто может менять статус?

---

## Связанные документы

| Документ | Путь |
|----------|------|
| Техническое задание для команды | `docs/technical-spec.md` |
| Интервью Novatska | `research/interview-novatska.md` |
| Интервью Shyfrin | `research/interview-shyfrin.md` |
| Прототип (статика) | https://alexshevnin-cas.github.io/creatives-catalog/ |
| Репозиторий | https://github.com/alexshevnin-cas/creatives-catalog |
